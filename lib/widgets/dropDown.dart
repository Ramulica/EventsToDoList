import 'package:flutter/material.dart';

class ChallangeRow extends StatelessWidget {
  const ChallangeRow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
